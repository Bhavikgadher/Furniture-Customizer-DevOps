apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgresql-pv
  namespace: furniture-customize
  labels:
    app: postgresql

spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /mnt/data/postgresql
  storageClassName: manual
